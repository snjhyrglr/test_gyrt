
<%= render @benefit %>

<div>
  <%= link_to "Edit", edit_benefit_path(@benefit), class: 'btn btn-primary' %> |
  <%= link_to "Back", benefits_path, class: 'btn btn-primary' %>

  <%= button_to "Delete", @benefit, method: :delete, class: 'btn btn-danger' %>
</div>
